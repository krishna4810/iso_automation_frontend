import { HeaderFormatPipe } from './header-format.pipe';

describe('HeaderFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new HeaderFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
